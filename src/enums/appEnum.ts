// menu theme enum
export enum ThemeEnum {
  DARK = 'dark',
  LIGHT = 'light'
}

// page switching animation
export enum PageTransitionEnum {
  FADE = 'fade',
  FADE_SCALE = 'fade-scale',
  ZOOM_FADE = 'zoom-fade'
}